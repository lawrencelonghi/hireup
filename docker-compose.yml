services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: air -c .air.toml
    volumes:
      - .:/app
      - /app/tmp
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=debug
    profiles:
      - dev
    restart: unless-stopped